<UserControl x:Class="Stockery.View.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stockery.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Stocks" Margin="10 10 10 10"/>
        <ListView Grid.Row="1" ItemsSource="{Binding Stocks}"
                  SelectedItem="{Binding SelectedStock,Mode=TwoWay}"
                  DisplayMemberPath="DisplayMember" />
        <Label Grid.Row="2" Content="Bonds" Margin="10 10 10 10"/>
        <ListView Grid.Row="3" ItemsSource="{Binding Bonds}"
                  SelectedItem="{Binding SelectedBond,Mode=TwoWay}"
                  DisplayMemberPath="DisplayMember"/>
    </Grid>
</UserControl>
